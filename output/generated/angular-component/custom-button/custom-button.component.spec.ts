import { ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import { CustomButtonComponent } from './custom-button.component';

/**
 * CustomButtonComponent 测试
 * Generated by MCP Generator at 2025/6/29 19:20:04
 */
describe('CustomButtonComponent', () => {
  let component: CustomButtonComponent;
  let fixture: ComponentFixture<CustomButtonComponent>;
  let debugElement: DebugElement;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [CustomButtonComponent]
    }).compileComponents();

    fixture = TestBed.createComponent(CustomButtonComponent);
    component = fixture.componentInstance;
    debugElement = fixture.debugElement;
    fixture.detectChanges();
  });

  it('应该创建组件', () => {
    expect(component).toBeTruthy();
  });

  it('应该正确显示组件名称', () => {
    const compiled = fixture.nativeElement as HTMLElement;
    const titleElement = compiled.querySelector('.custom-button__title');
    expect(titleElement?.textContent).toContain('CustomButton');
  });

  it('应该正确显示组件描述', () => {
    const compiled = fixture.nativeElement as HTMLElement;
    const descElement = compiled.querySelector('.custom-button__description');
    expect(descElement?.textContent).toContain('自定义按钮');
  });

  describe('输入属性测试', () => {
    it('应该正确接收  输入', () => {
      const testValue = 'test value';
      component. = testValue;
      fixture.detectChanges();
      expect(component.).toBe(testValue);
    });
  });






  it('应该应用正确的CSS类', () => {
    const compiled = fixture.nativeElement as HTMLElement;
    const mainDiv = compiled.querySelector('.custom-button');
    expect(mainDiv).toBeTruthy();
  });

  it('应该显示生成时间', () => {
    const compiled = fixture.nativeElement as HTMLElement;
    const footer = compiled.querySelector('.custom-button__footer small');
    expect(footer?.textContent).toContain('Generated by MCP Generator');
  });
}); 